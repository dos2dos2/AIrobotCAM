def select_strategy(ctx: MachiningContext):
    if ctx.features.curvature > 0.7:
        return "3D_contour"

    if ctx.features.pockets > 3:
        return "adaptive_clearing"

    if ctx.features.holes > 5:
        return "drilling"

    return "parallel"
